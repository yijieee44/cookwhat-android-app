<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cookwhat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookWhat">
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.CookWhat.noActionBar">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name">

             <intent-filter>
             <action android:name="android.intent.action.MAIN" />


             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>
        <activity
            android:name=".activities.ViewRecipeActivity"
            android:exported="true"
            android:label="@string/view"
            android:parentActivityName=".activities.ViewRecipeActivity"></activity>
        <activity
            android:name=".activities.UserActivity"
            android:exported="true"
            android:label="@string/view"
            android:parentActivityName=".activities.UserActivity"></activity>
        <activity
            android:name=".activities.SearchActivity"
            android:exported="true"
            android:label="@string/search"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.CreateActivity"
            android:exported="true"
            android:label="@string/create_recipe"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".followPopUp"
            android:theme="@style/CustomTheme" />
    </application>

</manifest>